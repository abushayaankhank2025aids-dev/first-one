#include <stdio.h>
int main()
{
    int a, b, c;
    printf("Enter a, b, c = ");
    scanf("%d%d%d", &a, &b, &c);
if(a>b)
{ if(a>c)
     { printf("a is greatest");}
  else
    {printf("c is greatest");}
}
else if(c>a)
{printf("c is greatest");}
else{printf("b is greater");}
return 0;
}
