// Online C compiler to run C program online
#include <stdio.h>

int main() {
int i,j=0,k;
scanf("%d",&i);
if(i==0)
{
printf("0");
return 0;
}
else
{
    while(i>0)
     {
         k=i%10;
    j=j+k;
    i=i/10;
     }
    
}
printf("%d",j);
    return 0;
}
